#!/bin/bash

# This script should be runable by running the following command in a bash terminal window:
#
# bash -c "$(curl -fsSL https://raw.githubusercontent.com/edrulrd/bash-setup/main/install.bash)"
#
# or
# bash -c "$(wget https://raw.githubusercontent.com/edrulrd/bash-setup/main/install.bash -O -)"

echo Hi Ed
echo This was run from github.com

neofetch

exit
